{
    // [a [INT, offset, D], b[INT, offset, D], c [INT, offset, I]]
    int a;
    int b;
    int c = 1;
    // [a [INT, offset, D], b[INT, offset, D], c [INT, offset, I]]
    if(c > 1){
        b = c;
        // [a [INT, offset, D], b[INT, offset, I], c [INT, offset, U], d [INT, offset, D]]

    } else {
          a = b;
          // [a [INT, offset, I], b[INT, offset, D->U->T], c [INT, offset, U]]
      }

    // Desiderata: [a [INT, offset, I], b[INT, offset,T], c [INT, offset, U]]

}