// Funzione ricorsiva per la valutazione del valore pari/dispari
{
    bool isEven (int value){
        if( value == 0){
            return true;
        } else {
            if (value == 1){
                return false;
            } else {
                return isEvens(num - 2);
            }
        }
    }

    bool num = 10;          // L'errore di tipo non verrà rilevato
    print (isEven(value));
}